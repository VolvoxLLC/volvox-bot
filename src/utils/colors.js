/**
 * Shared Discord embed color constants.
 * Centralises the palette used across commands and modules so every feature
 * uses consistent, named colors instead of scattered numeric literals.
 */

/** Discord brand / informational embeds (blurple). */
export const BLURPLE = 0x5865f2;

/** Warning / caution embeds (yellow). */
export const YELLOW = 0xfee75c;

/** Danger / negative / destructive-action embeds (red). */
export const RED = 0xed4245;

/** Success / positive embeds (green). */
export const GREEN = 0x57f287;
