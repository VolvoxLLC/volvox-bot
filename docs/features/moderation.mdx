---
title: Moderation
description: 'Auto-moderation, warnings, bans, timeouts, and case management'
---

# Moderation (Active Sentry)

Active Sentry is Volvox.Bot's moderation engine. It runs automatically and can be configured from the dashboard.

## What it does

- **Auto-moderation** — Automatically detect and act on spam, slurs, invite links, and more
- **AI Auto-Moderation** — Score incoming messages for toxicity, spam, harassment, hate speech, sexual content, violence, and self-harm, then run the configured action when a score meets its threshold
- **Warnings** — Issue warnings that accumulate and trigger escalating actions
- **Bans & Kicks** — Full ban and kick support with reason tracking
- **Timeouts** — Discord native timeouts with configurable durations
- **Cases** — Every moderation action creates a case with full context

## How to use

1. Open the dashboard → **Moderation** for your server
2. Review active cases, filter by type or member
3. Use **Config → Moderation** to set up classic auto-mod rules
4. Use **Config → AI & Automation → AI Auto-Moderation** to choose the detection model, thresholds, and actions

<Callout type="tip">
Configure escalation rules so that repeated warnings automatically escalate to timeouts or bans.
</Callout>

## Configuration

All moderation settings are in **Config → Moderation**:

| Setting | Description |
|---------|-------------|
| Auto-mod enabled | Toggle automatic moderation |
| Spam threshold | Messages per minute before flagging |
| Word filter | Custom list of blocked words |
| Invite blocking | Block Discord invite links |
| Log channel | Where moderation actions are posted |

AI moderation settings are in **Config → AI & Automation → AI Auto-Moderation**:

| Setting | Description |
|---------|-------------|
| Detection model | The provider/model used to score incoming messages |
| Incident report channel | Where flagged AI moderation reports are posted |
| Sensitivity thresholds | Per-category score required before action runs |
| Response actions | Ignore, delete, flag, warn, timeout, kick, or ban per category. `warn` creates a warning record, sends the warning DM when enabled, and checks escalation rules. |

Every triggered AI moderation response is recorded in the audit log as `ai_automod.<action>`, including `ai_automod.none` when a threshold is hit but the configured action is Ignore.

Changes apply instantly — no bot restart needed.
