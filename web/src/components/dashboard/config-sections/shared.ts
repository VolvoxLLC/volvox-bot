/**
 * Shared styling constants for config section form controls.
 *
 * Centralizes input/select class strings so they stay consistent
 * across all config section components.
 */

/** Shared input styling for text inputs and selects. */
export const inputClasses =
  'w-full rounded-md border bg-background px-3 py-2 text-sm ring-offset-background placeholder:text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50';
